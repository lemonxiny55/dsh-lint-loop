# dsh-lint-loop bundle: inserts the plugin row into the composition.
# Later layers (profile cordis.patch.yml, --patch) may override this row by id.
- insert:
    - id: lint-loop
      name: dsh-lint-loop
